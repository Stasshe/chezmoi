# ATTENTIONS — 過去session注意集約

- 対症療法禁止。根本原因直せ
- 後方互換・レガシー・fallback不要。消してクリーン実装（指示ない限り常時）
- 頼まれてない行動禁止。指示されたことだけ。「これだけ」=指定ファイル以外触るな
- 指示を正確に読め。対象取り違え多発（高さ/横幅、対象ファイル等）。曖昧→着手前確認
- 参照指定（md/html/URL/既存実装）は必ず読んで完全に従え
- バグ報告→まず自分のコード疑え。環境/キャッシュのせいにするな
- 大修正後は変更ファイル全再読→不要コード・不要修正を削除
- 固定値/特殊ケース分岐でなく一般化ロジック（例: span固定判定→面積計算）
- 過剰実装禁止。目的に必要十分（非セキュリティhashに厳格性不要）
- ごり押し自作の前に既存パッケージ/公式手法を探せ
- 出力/UIに自明ラベル禁止（"text changed"等）。具体的内容・該当箇所を出せ
- 不要な迂回手段（無意味なpython script等）使うな。直接やれ
- 情報渡されたら確認質問せず即作業。「進めろ」「一気に」=完遂まで止まるな
- 1ファイル800行上限。責務境界で分割。同レベルは同階層。分岐は読みやすく
- 提供資料の丸写し禁止。体系化して書き直せ
- 「デザイン刷新」=全く別物を作れ。似た物再生産禁止。AIっぽさ消せ
- workflows: 長いインラインscript禁止。古い手法でなく現行公式手法
- hooks/設定に mise/nvm 等の絶対パス書くな（環境で壊れる）
- デプロイ環境差（base path等）考慮
- 修正後に何が原因だったか一言で報告
