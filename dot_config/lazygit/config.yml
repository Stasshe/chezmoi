gui:
  theme:
    selectedLineBgColor:
      - "#45475a"
      - bold
    inactiveViewSelectedLineBgColor:
      - "#45475a"
      - bold
git:
  diffContextSize: 3000
  diffRenderers:
    - colorArg: always
      command: delta --dark --hunk-header-style=omit --line-numbers --paging=never
